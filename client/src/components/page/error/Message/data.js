import React from "react";
import { PROJECT_ROOT } from "utilities/constants/urls";
import LinkElement from "./LinkElement";

const data = {
  message: <><LinkElement to={`/${PROJECT_ROOT}`} render={"Click here to return"} /> to our home page!</>
};

export default data;